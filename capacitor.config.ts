import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.metalurgica.montenegro',
  appName: 'Metalurgica Montenegro',
  webDir: 'dist'
};

export default config;
